<h1>Editing Book</h1>

<% if @book.errors.any? %>
  <%= @book.errors.count %> error prohibited this book from being saved:
  <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>

<%= form_with model: @book do |f| %>

  <a>Title</a><br>
  <%= f.text_field :title %><br>
<br>
  <a>Body</a><br>
  <span><%= f.text_area :body %></span><br>

<br>
  <span><%= f.submit 'Update Book' %></span><br>
  <br>
  <span>
    <%= link_to "Show", book_path(@book.id) %> ｜
    <%= link_to "Back", "/books" %>
  </span>
<% end %>